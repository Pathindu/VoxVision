# ── Copy this file to .env and fill in your values ────────────────────────

# PostgreSQL connection string
DATABASE_URL=postgresql://voxvision:voxvision@localhost:5432/voxvision

# JWT – generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_ME_generate_a_real_secret_here
ACCESS_TOKEN_EXPIRE_MINUTES=60

# PayHere credentials (from your PayHere merchant dashboard)
PAYHERE_MERCHANT_ID=1235833
PAYHERE_MERCHANT_SECRET=Mjk2NTAxNjM5MjMzMDY1NjgzMzQxNjY2NzUxMTg5MjgyNzEyNzQxMQ==

# Google Gemini AI
GEMINI_API_KEY=AIzaSyCuoiORqFiqCZ6y_keBRwFv7Yc3vK4-W_Q
GEMINI_MODEL=gemini-2.0-flash-lite

# ── Google Cloud TTS ──────────────────────────────────────────────────────
# LOCAL: point to your downloaded service-account JSON key file
#GOOGLE_APPLICATION_CREDENTIALS=./cloud-tts-api.json
GOOGLE_APPLICATION_CREDENTIALS=./cloud-tts-api.json
APP_ORIGINS=https://klw94f7t-3000.asse.devtunnels.ms

# SERVER (Render / Railway): paste the entire JSON content of the key file as one line.
# The app will write it to a temp file automatically at startup.
# Leave blank when using GOOGLE_APPLICATION_CREDENTIALS above.
GOOGLE_CREDENTIALS_JSON=

# Frontend URL – added to CORS allowed origins (comma-separate multiple URLs)
# e.g.  APP_ORIGINS=https://your-app.onrender.com,https://your-custom-domain.com
APP_ORIGINS=http://localhost:3000
